# Cloudflare Worker that proxies the iOS app's Anthropic API calls.
#
# Setup (run from this directory):
#   1. wrangler login
#   2. wrangler kv namespace create DEVICES         # copy the id
#   3. wrangler kv namespace create CHALLENGES      # copy the id
#   4. wrangler kv namespace create RATE_LIMITS     # copy the id
#      Paste the three ids into the [[kv_namespaces]] entries below.
#   5. wrangler secret put ANTHROPIC_API_KEY        # paste the rotated key
#   6. Set APPLE_APP_ID below to "<TEAMID>.<bundle.id>" — find TEAMID at
#      developer.apple.com/account → Membership Details, bundle id matches
#      Xcode's Signing & Capabilities pane.
#   7. wrangler deploy

name = "calorie-calc-proxy"
main = "src/index.ts"
compatibility_date = "2025-01-15"

# Set this to "<TEAM_ID>.<BUNDLE_ID>" — used to verify App Attest attestations
# come from this app and not some other one signed with stolen credentials.
[vars]
APPLE_APP_ID = "X8629DMYVG.com.lawlerinnovationsinc-calorie"
# Per-device daily request quota. Abuse ceiling, not the paywall — sized so a
# compromised App Attest key can only run up ~$30/mo of Anthropic cost.
DAILY_REQUEST_LIMIT = "50"
# When true, accept the App Attest "appattestdevelop" AAGUID (local Xcode debug
# builds). Set "false" in production so only TestFlight/App Store builds (production
# attestation) register. Local debug builds against this worker will be rejected with
# bad_aaguid until this is flipped back or they're pointed at a separate dev worker.
ALLOW_DEV_ATTESTATION = "false"
# Credits granted per AdMob rewarded-video completion (verified via SSV in Phase 3).
CREDITS_PER_AD = "5"
# One-time grant fired on a device's first /v1/messages or /v1/account/state call
# after this version of the worker shipped. Doubles as the new-user free trial and
# the grandfather allotment for pre-existing TestFlight devices. Debug iOS builds
# override this to 1 via the X-Debug-Build header — see initialCreditsFor in index.ts.
INITIAL_FREE_CREDITS = "50"
# StoreKit 2 product id for the monthly subscription. Must match the product
# configured in App Store Connect exactly.
SUBSCRIPTION_PRODUCT_ID = "com.lawlerinnovationsinc_calorie.ai.monthly"
# Used to verify the `bundleId` claim in JWS receipts coming back from StoreKit.
APPSTORE_BUNDLE_ID = "com.lawlerinnovationsinc-calorie"
# Subscription environment gate, enforced in /v1/subscriptions/{verify,notify}: a
# receipt/notification whose `environment` claim doesn't match this is rejected, so a
# valid Sandbox (TestFlight) receipt can't grant a Production entitlement. Use
# "Production" for the live App Store; use "Sandbox" only on a worker dedicated to
# TestFlight subscription testing.
APPSTORE_ENVIRONMENT = "Production"
# Temporary "free AI for everyone" promo. "1" entitles every authenticated device for
# AI calls regardless of credits or subscription, and does NOT debit balances — meant
# to bridge the gap until the subscription + rewarded-ad updates clear App Store review.
# Set back to "0" and `wrangler deploy` to restore normal credit/subscription gating;
# every banked balance is exactly where it was. The DAILY_REQUEST_LIMIT abuse ceiling
# still applies while this is on.
PROMO_FREE_AI = "1"
# Optional per-flow model override, consulted only for requests that carry the
# X-AI-Flow header (app builds after build 3 — older builds are never affected).
# Keys: photo, describe, recipe-analyze, recipe-import, insights. Any flow missing
# here falls back to DEFAULT_MODEL_ROUTES in src/index.ts. Example:
# MODEL_ROUTES = '{"photo":"claude-sonnet-4-6","insights":"claude-haiku-4-5"}'

[[kv_namespaces]]
binding = "DEVICES"
id = "5a6501f17e33481d92ae7a5a99edda58"

[[kv_namespaces]]
binding = "CHALLENGES"
id = "cdb1656295a74c0a9c421bd931002b92"

[[kv_namespaces]]
binding = "RATE_LIMITS"
id = "d46f3bb76bf342bcb283844b60bbb606"

[observability]
enabled = true

# Daily SSV reward monitor. 13:00 UTC = 08:00 America/Chicago (CDT). The scheduled
# handler in src/index.ts reads ssv:stats via the native KV binding and posts a
# RED/YELLOW/GREEN verdict to ALERT_WEBHOOK_URL (set via `wrangler secret put`).
[triggers]
crons = ["0 13 * * *"]
